# Environment Configuration
NODE_ENV=production

# Database Configuration (Required)
DATABASE_URL=your_neon_database_url_here

# Session Security (Required - Generate a strong secret)
SESSION_SECRET=your_super_secure_session_secret_at_least_32_characters_long

# Email Configuration (Optional - for email notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
EMAIL_PASSWORD=your_app_password
ADMIN_EMAIL=admin@yourdomain.com

# Server Configuration
PORT=5000

# Security Notes:
# 1. Generate a strong SESSION_SECRET: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 2. Never commit this file to version control
# 3. Use environment variables in production hosting platforms